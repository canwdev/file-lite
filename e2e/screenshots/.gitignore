# 用例截图由测试运行时生成，不入库（历史提交里仍有，不回删）。
#
# 保留本文件是为了让新克隆出来的仓库里**存在一个空的 screenshots/ 目录**：
# 测试运行时往里写图不会因为目录不存在而失败，也不会留下一个未跟踪的空目录。
#
# README 功能表格用的截图不受影响——那些由 docs:screenshots 生成到
# ../docs/screenshots/，仍然提交进仓库。
*
!.gitignore

# File Lite

[中文](./README-cn.md) | English

<p align="center">
  <img src="frontend/public/favicon.webp" alt="File Lite" width="72" height="72" />
</p>

<p align="center"><b>Web file manager</b> · Vue 3 + TypeScript + Go</p>

---

![File Lite](docs/screenshots/00-main.webp)

|  |  |
| :-: | :-- |
| ![Tabs and split view](docs/screenshots/01-tabs-split.webp) | **Tabs & split view**: Open several folders at once and split a tab into two panes, side by side or stacked, then drag files straight from one pane into a folder of the other. |
| ![Transfers and background tasks](docs/screenshots/02-transfers-tasks.webp) | **Transfers & background tasks**: Uploads and downloads run in the browser while copy, move and delete run on the server, each a cancellable task with live progress in a docked panel. |
| ![Endless Gallery](docs/screenshots/03-gallery.webp) | **Endless Gallery**: A short-video-style vertical feed of the images, videos and audio in the current folder, with swipe, wheel and keyboard navigation. |
| ![Music player](docs/screenshots/04-music-player.webp) | **Music player**: Playlist, embedded cover art and synced lyrics, read from the track's own tags without downloading the whole file. |
| ![Video player](docs/screenshots/05-video-player.webp) | **Video player**: ArtPlayer or the native `<video>` element, switchable from the app menu and remembered per browser. |
| ![Previews and thumbnails](docs/screenshots/06-thumbnails.webp) | **Previews & thumbnails**: Image previews, video first frames (ffmpeg) and audio cover art are generated by the server and cached in the browser. |
| ![Text editor](docs/screenshots/07-text-editor.webp) | **Text editor**: Edit and save text files in place, with an unsaved-changes guard and an overwrite-safe save. |
| ![Properties window](docs/screenshots/08-properties.webp) | **Windows-style properties**: Icon, type, full path, sizes and dates, with a folder's recursive size counted in the background. |

- **Backend**: a single Go (Echo) server, shipped as one static binary with the UI embedded
- **Bundle size**: single artifact stays around **20MB** or less
- **Features**
  - Explorer: tabs and split view, list / grid, breadcrumbs, favourites, drives, hidden files, filter, per-path sort and layout
  - Files & folders: create, rename, move, copy, duplicate, delete, properties, per-extension default app
  - Transfers: drag & drop upload (files or folders), download, download a folder as ZIP; copy, move and delete are cancellable background tasks with progress and retry
  - Conflicts: replace, skip or keep both — per item or for the whole batch — and folders merge the way Windows Explorer does it
  - Previews: server-generated thumbnails — image previews, video first frames via ffmpeg, audio cover art — cached in the browser
  - Media: image viewer with zoom and rotate, video player, music player with playlist, embedded covers and synced lyrics
  - Endless Gallery: a short-video-style vertical feed of the current folder, with collect and a thumbnail strip
  - Editors & viewers: text editor, HTML viewer
  - Integration: embed File Lite as an Open File picker through a short-lived `ticket` URL, without a password
  - Utilities: speed test, realtime text sync
  - UI: light / dark / system themes with colour palettes, reduce motion, fullscreen, wake lock, touch-friendly
  - Maintenance: install a new backend binary, restart or stop the server from the Development menu
- **Security**
  - Password login issues JWT session tokens with a 1-year expiry
  - Console URLs use a short-lived `ticket` login parameter with a 2-minute TTL; printing URLs again generates a new `ticket`
  - “Remember login status” supports persistent cookies or browser-session cookies
  - Optional IP ban after repeated failed logins
  - Configurable allowed root path scope
  - Optional IP-range allowlist (`allowedCIDRs`)
  - HTTPS including self-signed certificates
  - Login attempt rate limiting

## Installation

Download the archive for your platform from [GitHub Releases](https://github.com/canwdev/file-lite/releases), unzip it and run the binary.

```shell
# Example: Linux amd64
unzip file-lite-linux_amd64-v*.zip
cd linux_amd64
./file-lite-go
```

## Development

Use **Bun** for the frontend and **Go 1.20+** for the backend.

```shell
# Backend: hot-reload dev server on port 3111
cd backend-go
bun i
bun run dev
```

```shell
# Frontend: Vite dev server on port 3110, proxies /api to the backend
cd frontend
bun i
bun run dev
```

```shell
# Package the current platform: builds the frontend, the Go binary and the release zip
cd backend-go
bun run build
```

- **Go backend**: build steps and `bun run build:all` are documented in [backend-go/README.md](backend-go/README.md)
- **E2E UI tests**: a Playwright sub-project drives the real app in a browser — [e2e/README.md](e2e/README.md); method and screenshots in [docs/design/frontend-ui-testing.md](docs/design/frontend-ui-testing.md)
- **README screenshots**: `cd e2e && bun run docs:screenshots` captures the table above from a demo library into [docs/screenshots](docs/screenshots)
- **File operations**: copy / move / delete run as cancellable background tasks over the WebSocket — design and implementation notes in [docs/design/async-file-operations-ws-design.md](docs/design/async-file-operations-ws-design.md)

## Configuration

- Config file path: `<cwd>/file-lite/config.json` (override the directory with `FILE_LITE_DATA_BASE_DIR`)
- [Configuration reference: every field and its default](./docs/config.md)
- Type reference: `Cfg` in [backend-go/config/config.go](backend-go/config/config.go)
- [Enable HTTPS with a self-signed certificate](./docs/ssl.md)
- [Restrict access to specific IP ranges (`allowedCIDRs`)](./docs/ip-allowlist.md)
- [Thumbnail and preview architecture](./docs/design/thumbnails.md)
- If `password` is empty, File Lite generates a random password; when the config file already exists, the generated password is written back
- `jwtToken` is the JWT signing secret; when an existing config file has an empty value, it is generated and written back
- The console does not print JWTs or signing secrets; check the config file for the login password

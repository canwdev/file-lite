# todo

这里只放**尚未实现**的方案，作为设计参考保留。

约定：

- 实现之后把文档移回 `docs/`，并把开头「状态」改成已实现、补上实现说明与测试覆盖——`docs/async-file-operations-ws-design.md` 就是这样一个例子（它记录了实现状态，也记录了后来主动删掉某块复杂度的原因）。
- 已经作废、被别的文档取代的方案直接删除，不要留在这里：过期文档比没有文档更容易误导。

当前内容：

| 文档 | 状态 |
| --- | --- |
| [frontend-multi-panel-design.md](./frontend-multi-panel-design.md) | 只剩多面板，方案搁置未实现；多选拖拽见 `ExplorerUI/entry-drag.ts`，多标签页已实现并拆到 [`docs/explorer-tabs-design.md`](../explorer-tabs-design.md) |

{
  "name": "file-lite",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch run src/main.ts",
    "build": "bun build --target=node --minify --outfile=dist/main.mjs ./src/main.ts",
    "prettier": "bunx prettier . --write"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^5.1.0",
    "get-port": "^7.1.0",
    "multer": "^2.0.2",
    "node-disk-info": "^1.3.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.1",
    "prettier": "3.6.2"
  }
}

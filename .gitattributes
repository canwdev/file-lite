# 行尾统一成 LF。
#
# Windows 上通常带 core.autocrlf=true，它会在 checkout 时把文本文件写成 CRLF。
# 于是「工具链按 LF 重写的文件」与「checkout 出来的 CRLF 文件」会来回翻转：
# 构建完 `git status` 报被改动、`git diff` 却一个字节都不显示（内容一致，
# 只是行尾不同）。固定 eol=lf 之后两边一致，不再有清不掉的噪声。
#
# text=auto 让 git 自己判断哪些是文本，二进制文件不受影响。
# 这一条已经覆盖了 *.sh（CI 里的脚本必须是 LF，CRLF 会让 Linux 上的 bash
# 报 "invalid option name"）与 .gitattributes 自己。
* text=auto eol=lf
